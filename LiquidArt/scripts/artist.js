(function(define, undefined) {
	
	'use strict';
	define('artist', [
		'core',
		'artist.header',
		'artist.summary',
		'artist.components',
		'artist.lightbox'
	],
	function(core, header, summary, components, lightbox, highlight){
	});
})(define);